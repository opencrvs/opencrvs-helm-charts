apiVersion: v1
data:
  reindex.sh: |
    {{- .Files.Get "files/reindex.sh" | indent 8 }}
kind: ConfigMap
metadata:
  labels:
    app: elasticsearch
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
  name: elasticsearch-reindex-script
